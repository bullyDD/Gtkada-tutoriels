with Ada.Text_IO;           use Ada.Text_IO;

procedure Main is
begin
    Put_Line ("Gtkada-tutorial serie :" & ASCII.CR &
    "This serie of tutorial shows the use of Gtkada which is Ada binding," & ASCII.CR &
    " of Gtk. We start from scratch on GUI creation. At the end we will build a simple game");
end Main;